#Hallar la matriz transpuesta
import numpy as np
#la funcion n.pe transpose crea la matriz transpuesta de una matriz
matriz = np.array([[1, 26, 36], [44, 1, 9], [14, 78, 13]])
transpuesta = np.transpose(matriz)
print(transpuesta)